name: check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run CI-portable checks
        run: make ci-check
